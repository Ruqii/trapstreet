{
  "name": "trapstreet",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm --filter @trapstreet/web dev",
    "build": "pnpm --filter @trapstreet/web build",
    "lint": "pnpm -r --if-present lint"
  }
}
